{
  "name": "svg-gradients",
  "version": "0.1.0",
  "license": "MIT",
  "description": "Playing with SVG radial gradients",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "serve": "vite preview",
    "predeploy": "rm -rf dist && yarn build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "vue": "^3.0.7",
    "vue-toastification": "^2.0.0-rc.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.1.5",
    "@vue/compiler-sfc": "^3.0.7",
    "sass": "^1.32.8",
    "typescript": "^4.2.3",
    "vite": "^2.1.2"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ],
  "keywords": []
}
